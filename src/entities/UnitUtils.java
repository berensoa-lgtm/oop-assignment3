/*
package entities;

import board.Position;

import java.util.HashMap;
import java.util.Map;


public class UnitUtils {

    public Runnable movement(Position pos,int rand) {

        Map<Integer, Runnable> movementMap = new HashMap<>();

        movementMap.put(1, () -> pos.up());
        movementMap.put(2, () -> Position.down());
        movementMap.put(3, () -> Position.left());
        movementMap.put(4, () -> Position.right());
        movementMap.put(5, () -> Position.stayPut());

        return movementMap
    }
}
*/
